#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <stdint.h>

int count = 2;
int base_time = 1000;
int freeze_time = 2000;

// in example we have 15 "Digital Input Port" in our board 
// apply chatter filter config to all of them and not individually
// array to keep each port freeze_status
// array to keep each port count of change
// array to keep each port last changed status time
// array to keep each port last changed status
// if time between to port change was bigger than base time we must reset 






// in "updateBuffersIn()" we must check if that port not in freeze mode


int main()
{

    return 0;
}